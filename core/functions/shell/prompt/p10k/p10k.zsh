s-prompt-p10k() {
    s-prompt-p10k-load
    s-prompt-p10k-configure

    # local p10k_dir="$(brew --prefix)/share/powerlevel10k"
    # [[ -d $p10k_dir ]] && source "$p10k_dir/powerlevel10k.zsh-theme"
}